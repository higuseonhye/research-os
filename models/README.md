# Models

Model weights are not stored in Git (see `.gitignore`).

EXP-SURG-001 Stage 1 smokes use a **scripted 6D IK-Rel proposer** in Isaac Sim — no trained policy weights are required to reproduce the committed results.
